; Template de configuração — este arquivo VAI para o git (sem senhas reais)
; Copie para config/.env e preencha com os valores reais do servidor

DB_HOST=mysql.seudominio.com.br
DB_NAME=nome_do_banco
DB_USER=usuario_banco
DB_PASS=senha_aqui
DB_PORT=3306
